{
  "name": "osmnearbynotesviewer",
  "version": "0.0.2",
  "description": "Reply to OSM Notes like comment form around home location.",
  "scripts": {
    "postinstall": "node -e \"require('shelljs').exec('typings install');\"",
    "prebuild": "node -e \"require('shelljs').exec('npm-run-all clean tslint:*');\"",
    "build": "node -e \"require('shelljs').exec('npm-run-all ts:build-* test pug mkdir-builddir browserify');\"",
    "postbuild": "node -e \"require('shelljs').exec('npm-run-all copy-*');\"",
    "clean": "node -e \"require('shelljs').rm('-R', ['temp', 'build']);\"",
    "copy-src": "node -e \"require('shelljs').cp('-R', 'src/css', 'build')\"",
    "tslint:browser": "node -e \"require('shelljs').exec('tslint ./src/ts/browser/**.ts');\"",
    "jade": "node -e \"require('shelljs').exec('jade src/views -P -o build');\"",
    "pug": "node -e \"require('shelljs').exec('pug -P src/pug --out build');\"",
    "mkdir-builddir": "node -e \"require('shelljs').mkdir('build/js');\"",
    "browserify": "node -e \"require('shelljs').exec('browserify --entry ./temp/browser/main.js -o build/js/main.js');\"",
    "ts:build-browser": "tsc -p ./src/ts/browser",
    "test": "node -e \"require('shelljs').exec('mocha --require ./src/test/espower-typescript-loader src/test/**.ts');\""
  },
  "keywords": [
    "OpenStreetMap",
    "OSM"
  ],
  "author": "tom-konda",
  "license": "MIT",
  "dependencies": {
    "es6-promise": "^3.1.2",
    "leaflet.icon.glyph": "^0.2.0",
    "osm-auth": "^0.2.8"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "espower-typescript": "^4.1.0",
    "jade": "^1.11.0",
    "mocha": "^2.4.5",
    "node-sass": "^3.4.2",
    "npm-run-all": "^1.5.1",
    "oauth": "^0.9.14",
    "power-assert": "^1.2.0",
    "pug": "^2.0.0-alpha6",
    "pug-cli": "^1.0.0-alpha1",
    "shelljs": "^0.6.0",
    "should": "^8.2.2",
    "tslint": "^3.5.0",
    "tslint-eslint-rules": "^1.0.1",
    "typedoc": "^0.3.12",
    "typings": "^0.7.12",
    "xml2js": "^0.4.16"
  },
  "engines": {
    "node": ">=4.3.0"
  }
}
